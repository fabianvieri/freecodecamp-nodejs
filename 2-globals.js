// !! GLOBALS -- NO WINDOW
// ? globals -> anywhere in the app we can access
// ? __dirname - path to current dir
// ? __filename - file name
// ? require - function to use module (CommonJS)
// ? module - info aboput current module(file)
// ? process - info about the env where the node program is executed

console.log(__dirname);
